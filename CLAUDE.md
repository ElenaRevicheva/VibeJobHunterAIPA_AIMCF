# CLAUDE.md — VibeJobHunter AI Job Hunting Engine
> Auto-generated by CTO AIPA on 2026-03-27 via full codebase scan.
> Working dir: `D:\aideazz\VibeJobHunterAIPA_AIMCF`
> Production: Oracle Cloud VM (us-chicago-1), systemd `vibejobhunter`

---

## What This Repo Is

VibeJobHunter is an autonomous AI job-hunting engine built in Python. It runs 24/7 on Oracle Cloud and performs the full hiring pipeline without human input:

1. **Discover** — scrapes 10 sources (218 ATS company APIs, Dice MCP, YC, RemoteOK, Wellfound, WWR, HN, AI Jobs, Torre.ai LATAM, Himalayas global remote)
2. **Score** — Claude AI dimensional scoring (100 pts) + bias compensation + dedup cache
3. **Route** — Score ≥ 60 → ATS auto-apply | 58–59 → founder outreach | 55–57 → review queue | < 55 → discard
4. **Apply** — Playwright ATS form submission (Greenhouse, Lever, Ashby, Workable); selects 1 of 6 resume variants
5. **Outreach** — finds founder emails/LinkedIn; generates hyper-personalized multi-channel messages
6. **Follow-up** — auto follow-up after 5 days of silence
7. **Notify** — Telegram bot reports every cycle result to Elena
8. **Brand** — LinkedIn CMO agent posts bilingual (EN/ES) content daily

---

## Directory Structure

```
src/
├── main.py                   # CLI entry point (Click) — setup / search / apply / autonomous / batch
├── core/
│   ├── config.py             # Settings (pydantic-settings) — roles, salary floors, links
│   ├── models.py             # Pydantic models: Profile, JobPosting, Application
│   └── profile_manager.py   # Load/save Elena's candidate profile
├── agents/
│   ├── job_matcher.py        # Claude AI dimensional scoring v3.0 + bias compensation
│   ├── founding_engineer_scorer.py  # Extra scorer for founding engineer positions
│   ├── content_generator.py  # Resume tailoring + cover letter generation
│   ├── content_generator_v2.py
│   └── application_manager.py      # Save/retrieve applications, daily stats
├── autonomous/
│   ├── orchestrator.py       # 🧠 THE BRAIN — full 24/7 cycle coordination (v4.3)
│   ├── job_monitor.py        # Job discovery across all sources + dedup cache
│   ├── company_researcher.py # AI company research (stage, mission, stack)
│   ├── founder_finder_v2.py  # Real email + LinkedIn discovery for founder outreach
│   ├── message_generator.py  # Hyper-personalized outreach messages
│   ├── multi_channel_sender.py  # LinkedIn + email + Twitter send
│   ├── ats_submitter.py      # Playwright ATS form automation
│   ├── follow_up_engine.py   # Auto follow-up scheduling + send
│   ├── response_detector.py  # Detects replies in email/LinkedIn
│   ├── response_handler.py   # Classifies responses (positive/negative/interview)
│   ├── demo_tracker.py       # Tracks demo link clicks → hot leads
│   ├── warm_intro_queue.py   # Warm intro management
│   ├── rate_limiter.py       # Per-channel rate limiting
│   ├── job_gate.py           # Domain filter (drops DevOps, QA, DBA roles)
│   └── target_companies.py   # Priority company list
├── scrapers/
│   ├── ats_scraper.py        # 218 company ATS APIs (Greenhouse, Lever, Ashby, Workable)
│   ├── dice_mcp_client.py    # Dice.com via Model Context Protocol
│   ├── linkedin_scraper.py   # LinkedIn job search
│   ├── indeed_scraper.py     # Indeed job search
│   ├── base_scraper.py       # Shared scraper base class
│   └── premium_board_scraper.py  # RemoteOK, Wellfound, WWR, AI Jobs, HN
├── filters/
│   ├── criteria_matcher.py   # Salary, location, remote filters
│   └── red_flag_detector.py  # Detects misaligned roles (WordPress, ads, etc.)
├── templates/
│   ├── resume_*.md           # 6 role-specific resume variants (EN + ES)
│   ├── resume_selector.py    # Picks the right variant per job
│   ├── resume_formatter.py   # Converts .md → PDF-ready
│   ├── cover_letter_formatter.py
│   └── yc_application_templates.json
├── notifications/
│   ├── telegram_bot_enhanced.py  # Telegram bot interface (python-telegram-bot 20.7)
│   ├── telegram_notifier.py      # Simple one-way notifications
│   ├── telegram_qa_helper.py     # Q&A mode for Telegram commands
│   ├── linkedin_cmo_v4.py        # LinkedIn CMO — daily bilingual posts
│   └── performance_tracker.py
├── enhancers/
│   ├── follow_up_scheduler.py
│   ├── interview_prep_generator.py
│   └── portfolio_integrator.py
├── dashboard/
│   ├── tracker.py            # Job hunt tracking dashboard (Rich CLI)
│   └── performance_tracker.py
├── api/
│   ├── app.py                # FastAPI web server (dashboard routes)
│   └── ga_dashboard_routes.py  # GA4 analytics integration
├── database/
│   └── database_models.py    # SQLAlchemy models (local SQLite in autonomous_data/)
├── search/
│   └── yc_angellist_search.py
├── loaders/
│   └── candidate_loader.py   # Loads Elena's profile from candidate_data.json
└── utils/
    ├── claude_helper.py      # Anthropic API wrapper
    ├── cache.py              # Seen-jobs dedup cache
    ├── rate_limiter.py       # API rate limiting
    ├── retry.py              # Retry with backoff
    ├── progress_saver.py     # Resume interrupted batch runs
    └── logger.py
```

---

## Scoring System (Job Matcher v3.0 — calibrated 2026-03-30)

| Dimension | Points | What it measures |
|-----------|--------|-----------------|
| AI product ownership | 25 | Does the role own an AI product end-to-end? |
| 0→1 autonomy / founding | 25 | Solo builder, founding engineer signals |
| Full-stack / infra | 20 | Python/TS + DB + deployment experience |
| Business exposure | 15 | Revenue ownership, cross-functional work |
| Bilingual / global | 5 | EN/ES bonus |
| Web3 (optional) | 10 | Polygon, IPFS, DAO signals |

**Routing thresholds (evidence-tuned 2024-12-18):**
- ≥ 60 → ATS auto-apply
- 58–59 → founder outreach
- 55–57 → review queue
- < 55 → discard

**Penalty layers (added 2026-03-29/30 — career analysis calibration):**
- Level 0: US-only eligibility → -60 (Elena is in Panama, not US-authorized)
- Level 1: Wrong role (payroll, CAD, finance, logistics) → -40
- Level 1b: Wrong role in description (2+ matches) → -30
- Level 2: Not her lane (DevOps, QA, DBA, data scientist) → -20
- Level 2b: Wrong stack (Java, C#, .NET, Go, PHP, Ruby, Rust) → -15
- Level 3: No domain signal in title → -15 / -10
- IT outsourcer penalty (Nagarro, Infosys, Gainwell, etc.) → -10
- LATAM-friendly boost (global remote, LATAM, anywhere in world) → +4 / +8
- **AI gate**: if penalty ≤ -20, Claude deep analysis is skipped entirely (prevents AI from overriding wrong-role signals — root cause of Jan–Mar 2026 bad applications)

**Career analysis note:** The scoring threshold (≥60 → auto-apply) is sound. The risk was never the threshold — it was the role categories passing Phase 0. A 75-point score on "Senior AI Engineer" at a 50-person company is still a rabbit hole for Elena's profile.

YC company bonus: +15 pts applied before routing.

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Language | Python 3.11 |
| AI — Primary | Anthropic Claude (anthropic>=0.25.0) |
| AI — Secondary | OpenAI (content generation fallback) |
| AI — Framework | LangChain + LangChain-Anthropic (imported, partial use) |
| Bot | python-telegram-bot 20.7 |
| Browser automation | Playwright (ATS form submission) |
| Web framework | FastAPI + Uvicorn (dashboard) |
| DB | SQLAlchemy 2.0 + SQLite (local `autonomous_data/`) |
| Data validation | Pydantic v2 + pydantic-settings |
| Email | Resend 0.8.0 |
| LinkedIn | linkedin-api 2.2.0 |
| Twitter | Tweepy 4.14.0 |
| Analytics | Google Analytics Data API (GA4) |
| Scheduling | `schedule` library (inside orchestrator loop) |
| CLI | Click + Rich |
| Process manager | systemd (Oracle) |
| Infra | Oracle Cloud VM, us-chicago-1, $0/month |

---

## How to Run

### Local dev
```bash
cd D:\aideazz\VibeJobHunterAIPA_AIMCF
cp .env.example .env   # add keys
pip install -e .       # or: pip install -r requirements.txt

# Load Elena's profile (instant)
python -m src.main setup --elena

# Single search cycle
python -m src.main search -k "AI Engineer" --remote

# Batch apply from URL list
python -m src.main batch --file jobs.txt --v2

# Full autonomous mode (runs indefinitely)
python -m src.main autonomous --interval 1 --max-jobs 10
```

### On Oracle (production)
```bash
# SSH in (details in .env.private)
ssh -i $ORACLE_SSH_KEY ubuntu@$ORACLE_IP

# Check status
systemctl status vibejobhunter
journalctl -u vibejobhunter -n 50 --no-pager

# Deploy update
cd /home/ubuntu/VibeJobHunterAIPA_AIMCF
git pull
pip install -r requirements.txt
systemctl restart vibejobhunter
```

---

## Environment Variables (`.env`)

```
ANTHROPIC_API_KEY=         # Required — job scoring + content generation
OPENAI_API_KEY=            # Optional — fallback content generation
TELEGRAM_BOT_TOKEN=        # Telegram bot for cycle notifications
TELEGRAM_CHAT_ID=          # Elena's Telegram chat ID
LINKEDIN_EMAIL=            # LinkedIn account for scraping/posting
LINKEDIN_PASSWORD=
RESEND_API_KEY=            # Email outreach via Resend
RESUME_PATH=               # Path to PDF resume for ATS submissions
GA4_PROPERTY_ID=           # Google Analytics 4 for dashboard
USE_AI_JOB_ANALYSIS=true   # Feature flag: enable Claude deep scoring
```

---

## Known Issues & Tech Debt

| Issue | Severity | Notes |
|-------|----------|-------|
| **Eval framework — complete** | ✅ DONE | All 4 layers built and green (131 tests, ~43s). Layer 4 = Claude-as-judge consistency (12 curated cases + 22 golden set, ≥75% agreement). Cost: ~$0.03/run. |
| **Scoring calibration — career analysis alignment** | 🟡 MED | Career analysis says "stop applying to Senior/Staff/Principal at 20+ companies." Current engine has penalty layers but no hard company-size gate. The penalty system (-40/-20/-15) catches most wrong roles but doesn't enforce the "5–100 employees" hard gate from CAREER_FOCUS.md. Surgical edit territory. |
| **LangChain imported but underused** | 🟡 MED | `langchain` is in requirements but most LLM calls go direct via `anthropic` SDK. Either commit or remove. |
| **SQLite in production** | 🟡 MED | `autonomous_data/` SQLite works but isn't the Oracle ATP used by other agents. Inconsistency. |
| **LinkedIn scraper fragile** | 🟡 MED | `linkedin-api` unofficial library — breaks on LinkedIn bot detection changes. |
| **No monitoring / alerting** | 🟡 MED | Telegram notifications exist but no structured error rate tracking or uptime monitoring. |
| **Railway references** | 🟡 LOW | `config.py` checks `RAILWAY_ENVIRONMENT`; `railway.json` still in repo. Stale — all infra is Oracle. |
| **Playwright not headless-tested on Oracle** | 🟡 MED | ATS form submission requires browser — verify Playwright headless works on Oracle VM. |
| **No dedup across resume variants** | 🟡 LOW | If the same job is applied twice with a different resume variant, there's no guard. |

---

## Files NOT to Touch Without Reading First

| File | Why |
|------|-----|
| `src/autonomous/orchestrator.py` | Core production logic. Any change affects live 24/7 cycle. |
| `src/agents/job_matcher.py` | Scoring thresholds are evidence-tuned — don't adjust without eval data. |
| `src/core/config.py` | Salary floors and target roles are deliberate — changes affect all scoring. |
| `src/notifications/linkedin_cmo_v4.py` | Posts to LinkedIn automatically — test in staging before touching. |
| `src/autonomous/ats_submitter.py` | Submits real job applications — test mode must be on during dev. |

---

## Build Priority (aligned with career analysis v2 — 2026-03-30)

0. **Audit auto-apply targets** — ✅ Done. Wrong-stack, IT outsourcer, US-only, AI gate fixes deployed.
1. **Eval framework** — ✅ Done. All 4 layers: keyword (L1), bias compensation (L2), golden set routing (L3), LLM-as-judge consistency (L4). 131 tests, ~43s, ~$0.03/run.
2. **README: tool-use design section** — document which tools are called when and why
3. **README: monitoring/eval section** — show production-level thinking
4. **Remove Railway artefacts** — clean `railway.json`, `railway-entrypoint.sh`, `RAILWAY_ENVIRONMENT` env check
5. **SQLite → Oracle ATP** — align DB with rest of the AIdeazz ecosystem

---

## Positioning Rule (from career analysis v2)

When presenting Elena's background in any context:
1. Lead with Phase 1 (executive, 7 years board-level) + Phase 2 (AI builder, 9 production systems) — the hybrid is the differentiator
2. Never claim "Senior AI Engineer" for roles that filter on 5+ years conventional tenure
3. For fractional pitches: "executive who ships AI systems" is the pitch
4. EspaLuz: "early paid users" — not "revenue-generating product at scale"
5. LangChain: "exposure" — not a core skill
6. RAG: honest gap — do not claim partial without production evidence

## Interview Talking Points (from this codebase + career analysis v2)

- **Lead with the hybrid:** "I'm an executive-turned-AI-builder — 7 years running digital infrastructure at board level, past year shipping 9 production AI systems. I can talk to a CEO and a developer in the same conversation."
- **"I built a dimensional scoring system"** — 6-dimension, 100-point job→profile match with bias compensation + 7-level penalty system, evidence-tuned thresholds, not just keyword matching
- **"131-test eval harness, 4 layers"** — keyword scoring, bias compensation, golden-set routing, LLM-as-judge consistency. Layers 1–3: deterministic, $0, <1s. Layer 4: Claude-as-judge validates engine agrees with informed LLM ≥75%. Production regressions caught before deploy.
- **"Model routing by signal type"** — Claude for deep analysis; lightweight rules for domain filtering; cost-controlled by `USE_AI_JOB_ANALYSIS` flag. AI gate prevents expensive Claude calls on obviously-wrong jobs.
- **"Production-grade autonomous loop"** — systemd managed, runs every hour, Telegram notifications, self-healing
- **"ATS automation with Playwright"** — form submission across Greenhouse, Lever, Ashby, Workable (real applications, not just URL collection)
- **"6 resume variants, auto-selected"** — role-specific resume selection based on job signal, not one-size-fits-all

---

> This file was generated by CTO AIPA after a full codebase scan.
> It replaces any stale documentation. Update it when architecture changes.
> Last scan: 2026-03-30 — aligned with elena_career_analysis_v2.html (Honest Edition)
